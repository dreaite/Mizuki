---
import { Icon } from "astro-icon/components";

interface Props {
	prevUrl?: string;
	prevTitle?: string;
	nextUrl?: string;
	nextTitle?: string;
	class?: string;
}

const {
	prevUrl,
	prevTitle,
	nextUrl,
	nextTitle,
	class: className,
} = Astro.props;

const onlyOneButton = (prevUrl && !nextUrl) || (!prevUrl && nextUrl);
---

<div
	class:list={[
		"flex flex-col mb-4 gap-4 overflow-hidden w-full",
		className,
		!onlyOneButton && "md:flex-row md:justify-between",
	]}
>
	{
		prevUrl && (
				<a
				href={prevUrl}
				class="w-full font-bold overflow-hidden active:scale-95"
			>
				<div class="btn-card rounded-2xl w-full h-15 max-w-full px-4 flex items-center justify-start! gap-4">
					<Icon
						name="material-symbols:chevron-left-rounded"
						class="text-[2rem] text-(--primary)"
					/>
					<div class="overflow-hidden transition text-ellipsis whitespace-nowrap max-w-[calc(100%-3rem)] text-black/75 dark:text-white/75">
						{prevTitle}
					</div>
				</div>
			</a>
		)
	}

	{
		nextUrl && (
				<a
				href={nextUrl}
				class="w-full font-bold overflow-hidden active:scale-95"
			>
				<div class="btn-card rounded-2xl w-full h-15 max-w-full px-4 flex items-center justify-end! gap-4">
					<div class="overflow-hidden transition text-ellipsis whitespace-nowrap max-w-[calc(100%-3rem)] text-black/75 dark:text-white/75">
						{nextTitle}
					</div>
					<Icon
						name="material-symbols:chevron-right-rounded"
						class="text-[2rem] text-(--primary)"
					/>
				</div>
			</a>
		)
	}
</div>
